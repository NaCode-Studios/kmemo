name: build

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    # Every action below is pinned to a major that declares `using: node24`. Node 20 is deprecated
    # on GitHub-hosted runners, and the previous majors (checkout@v4, setup-java@v4,
    # setup-gradle@v4, upload-artifact@v4) still target it — they only run because the runner
    # force-upgrades them. Do not lower these majors without checking `runs.using` first.
    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Build and test
        run: ./gradlew build --stacktrace

      # The corpus report is the project's headline claim. Keeping it in the build log means the
      # numbers in the README can always be checked against a real run.
      - name: Near-miss corpus report
        run: ./gradlew :kmemo-core:test --tests '*NearMissCorpusTest*' --rerun-tasks

      - name: Upload test report
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: test-report
          path: kmemo-core/build/reports/tests/test
